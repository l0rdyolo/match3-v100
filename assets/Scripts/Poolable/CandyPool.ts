import { _decorator, Component, Node, Prefab } from 'cc';
import { Poolable } from '../../SooLib/Optimization/Poolable';
const { ccclass, property } = _decorator;

@ccclass('CandyPool')
export class CandyPool extends Poolable {
    start() {

    }

    update(deltaTime: number) {
        
    }
}

